<?xml version="1.0" encoding="utf-8"?>

<UserControl
    x:Class="CroomsBellScheduleCS.UI.Views.Settings.AnnouncementsView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:controls="using:CommunityToolkit.WinUI.Controls"
    mc:Ignorable="d" Loaded="UserControl_Loaded" Height="400">

    <Grid>
        <ScrollViewer x:Name="ContentArea" Visibility="Collapsed" Height="300">
            <controls:WrapPanel x:Name="ContentBox" Orientation="Vertical">

            </controls:WrapPanel>
        </ScrollViewer>
        <StackPanel
    x:Name="Loader" Padding="50">
            <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
                <ProgressRing x:Name="LoadingThing"/>
                <TextBlock x:Name="LoadingText">Loading...</TextBlock>
            </StackPanel>
            
        </StackPanel>
    </Grid>
</UserControl>