<?xml version="1.0" encoding="utf-8"?>

<Page
    x:Class="CroomsBellScheduleCS.UI.Views.Settings.ProwlerProfileView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:controls="using:Microsoft.UI.Xaml.Controls"
    mc:Ignorable="d">

    <Grid>
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <Image x:Name="FlyoutBanner" Grid.Row="0" Height="175" Stretch="UniformToFill" Margin="0"/>

            <PersonPicture x:Name="FlyoutPicture" Width="64" Height="64" VerticalAlignment="Top" HorizontalAlignment="Left" Grid.Row="1" Padding="0" Margin="0, -50, 10, 0" />

            <StackPanel Grid.Row="2">
                <TextBlock x:Name="FlyoutUsername" Text="User Account"  FontSize="22" Margin="0, 0, 0, 0" 
             Padding="12, 5, 10, 5" TextWrapping="Wrap"/>
            </StackPanel>
        </Grid>
        <ProgressRing x:Name="LoadingRing" HorizontalAlignment="Center" VerticalAlignment="Center"/>
    </Grid>
</Page>