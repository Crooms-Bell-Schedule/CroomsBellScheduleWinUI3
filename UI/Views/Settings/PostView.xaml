<?xml version="1.0" encoding="utf-8"?>

<UserControl
    x:Class="CroomsBellScheduleCS.UI.Views.Settings.PostView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid>
        <StackPanel
            x:Name="ContentArea">

            <RelativePanel Margin="0,0,0,20" HorizontalAlignment="Stretch">
                <RelativePanel.Resources>
                    <Style TargetType="Button">
                        <Setter Property="BorderThickness" Value="0" />
                        <Setter Property="Background" Value="Transparent"/>
                        <Setter Property="Margin" Value="0,0,8,0" />
                    </Style>
                </RelativePanel.Resources>

                <Button x:Name="BoldButton" AutomationProperties.Name="Bold" ToolTipService.ToolTip="Bold" Click="BoldButton_Click">
                    <Button.Content>
                        <FontIcon Glyph="&#xE8DD;"/>
                    </Button.Content>
                </Button>
                <Button x:Name="italicButton" Click="ItalicButton_Click" AutomationProperties.Name="Italic" ToolTipService.ToolTip="Italic"
            RelativePanel.RightOf="BoldButton">
                    <Button.Content>
                        <FontIcon Glyph="&#xE8DB;"/>
                    </Button.Content>
                </Button>

                <Button x:Name="NumberedList" Click="BulletList_Click" AutomationProperties.Name="Bullet List" ToolTipService.ToolTip="Bullet List"
RelativePanel.RightOf="italicButton">
                    <Button.Content>
                        <FontIcon Glyph="&#xE8FD;"/>
                    </Button.Content>
                </Button>

                <Button x:Name="InsertLink" AutomationProperties.Name="Insert link" ToolTipService.ToolTip="Insert link"
RelativePanel.RightOf="NumberedList">
                    <Button.Content>
                        <FontIcon Glyph="&#xE71B;"/>
                    </Button.Content>

                    <Button.Flyout>
                        <Flyout x:Name="LinkFlyout" Opened="LinkFlyout_Opened">
                            <StackPanel>
                                <TextBox x:Name="LinkFlyoutURL" PlaceholderText="URL"/>
                                <TextBlock x:Name="LinkFlyoutError" Foreground="Red"/>
                                <Button Content="Insert" Click="LinkFlyoutInsert_Click"/>
                            </StackPanel>
                        </Flyout>
                    </Button.Flyout>
                </Button>

                 <Button x:Name="ClearFormatting" AutomationProperties.Name="Clear Formatting" ToolTipService.ToolTip="Clear formatting"
RelativePanel.RightOf="InsertLink" Click="ClearFormatting_Click">
                    <Button.Content>
                        <FontIcon Glyph="&#xED62;"/>
                    </Button.Content>
                </Button>

                <DropDownButton x:Name="fontColorButton" AutomationProperties.Name="Font color"
                                BorderThickness="0" ToolTipService.ToolTip="Font color"
                                Background="Transparent"
                                RelativePanel.RightOf="NumberedList"
                                Visibility="Collapsed">
                    <SymbolIcon Symbol="FontColor"/>
                    <DropDownButton.Flyout>
                        <Flyout Placement="Bottom">
                            <VariableSizedWrapGrid Orientation="Horizontal" MaximumRowsOrColumns="3">
                                <VariableSizedWrapGrid.Resources>
                                    <Style TargetType="Rectangle">
                                        <Setter Property="Width" Value="32"/>
                                        <Setter Property="Height" Value="32"/>
                                    </Style>
                                    <Style TargetType="Button">
                                        <Setter Property="Padding" Value="0"/>
                                        <Setter Property="MinWidth" Value="0"/>
                                        <Setter Property="MinHeight" Value="0"/>
                                        <Setter Property="Margin" Value="6"/>
                                    </Style>
                                </VariableSizedWrapGrid.Resources>
                                <Button Click="ColorButton_Click" AutomationProperties.Name="Red">
                                    <Button.Content>
                                        <Rectangle Fill="Red"/>
                                    </Button.Content>
                                </Button>
                                <Button Click="ColorButton_Click" AutomationProperties.Name="Orange">
                                    <Button.Content>
                                        <Rectangle Fill="Orange"/>
                                    </Button.Content>
                                </Button>
                                <Button Click="ColorButton_Click" AutomationProperties.Name="Yellow">
                                    <Button.Content>
                                        <Rectangle Fill="Yellow"/>
                                    </Button.Content>
                                </Button>
                                <Button Click="ColorButton_Click" AutomationProperties.Name="Green">
                                    <Button.Content>
                                        <Rectangle Fill="Green"/>
                                    </Button.Content>
                                </Button>
                                <Button Click="ColorButton_Click" AutomationProperties.Name="Blue">
                                    <Button.Content>
                                        <Rectangle Fill="Blue"/>
                                    </Button.Content>
                                </Button>
                                <Button Click="ColorButton_Click" AutomationProperties.Name="Indigo">
                                    <Button.Content>
                                        <Rectangle Fill="Indigo"/>
                                    </Button.Content>
                                </Button>
                                <Button Click="ColorButton_Click" AutomationProperties.Name="Violet">
                                    <Button.Content>
                                        <Rectangle Fill="Violet"/>
                                    </Button.Content>
                                </Button>
                                <Button Click="ColorButton_Click" AutomationProperties.Name="Gray">
                                    <Button.Content>
                                        <Rectangle Fill="Gray"/>
                                    </Button.Content>
                                </Button>
                                <Button Click="ColorButton_Click" AutomationProperties.Name="Black">
                                    <Button.Content>
                                        <Rectangle Fill="Black"/>
                                    </Button.Content>
                                </Button>
                                <Button Click="ColorButton_Click" AutomationProperties.Name="White">
                                    <Button.Content>
                                        <Rectangle Fill="White"/>
                                    </Button.Content>
                                </Button>
                            </VariableSizedWrapGrid>
                        </Flyout>
                    </DropDownButton.Flyout>
                </DropDownButton>

                <RichEditBox x:Name="PostContentBox" PlaceholderText="Enter post content"
                             TextChanged="PostContentBox_TextChanged" 
                             IsSpellCheckEnabled="True" AcceptsReturn="True" TextWrapping="Wrap"
                             IsTextPredictionEnabled="True" Height="200" 
                              AutomationProperties.Name="Custom editor"
                    RelativePanel.Below="BoldButton"
                    RelativePanel.AlignLeftWithPanel="True"
                    RelativePanel.AlignRightWithPanel="True"
                    GotFocus="Editor_GotFocus"/>
            </RelativePanel>
            

            <TextBlock x:Name="LoginFailureText" Foreground="{ThemeResource SystemFillColorCriticalBrush}"></TextBlock>
        </StackPanel>
        <StackPanel
    x:Name="Loader" Visibility="Collapsed">
            <ProgressRing x:Name="LoadingThing" VerticalAlignment="Center" HorizontalAlignment="Center"/>
        </StackPanel>
    </Grid>
</UserControl>