<?xml version="1.0" encoding="utf-8"?>

<Page
    x:Class="CroomsBellScheduleCS.UI.Views.Settings.WebView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:controls="using:Microsoft.UI.Xaml.Controls"
    mc:Ignorable="d"
    Loaded="Page_Loaded">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Grid Grid.Row="0">
            <Button Content="Return" Click="Return_Click" x:Name="ButtonReturn"></Button>
            <HyperlinkButton x:Name="OpenInBrowser" Content="Open in browser" VerticalAlignment="Center" HorizontalAlignment="Right"></HyperlinkButton>
        </Grid>
        
         <controls:WebView2 x:Name="TheWebView"  Grid.Row="1" Grid.ColumnSpan="2"
        Source="https://mikhail.croomssched.tech/advice" HorizontalAlignment="Stretch" 
        VerticalAlignment="Stretch" Margin="0,5,0,0" Visibility="Collapsed"/>

        <StackPanel x:Name="Loader" Grid.Column="0" Grid.Row="0" Grid.RowSpan="2" Grid.ColumnSpan="2" VerticalAlignment="Center" HorizontalAlignment="Center">
            <ProgressRing x:Name="LoadingThing" Height="64" Width="64"/>
            <TextBlock x:Name="LoadingText" HorizontalAlignment="Center" Margin="0,5,0,0">Initializing WebView2</TextBlock>
        </StackPanel>
    </Grid>
</Page>