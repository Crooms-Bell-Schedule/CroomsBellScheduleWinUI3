<?xml version="1.0" encoding="utf-8"?>

<Page
    x:Class="CroomsBellScheduleCS.Views.Settings.FeedView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:local="using:CroomsBellScheduleCS.Utils"
    xmlns:views="using:CroomsBellScheduleCS.Views.Settings"
    mc:Ignorable="d" Loaded="Page_Loaded">


    <Grid>
        <Grid x:Name="FeedUI">
            <Grid.RowDefinitions>
                <RowDefinition Height="auto"></RowDefinition>
                <RowDefinition Height="*"></RowDefinition>
            </Grid.RowDefinitions>
            <StackPanel Orientation="Horizontal" Grid.Row="0">
                <TextBlock VerticalAlignment="Center" FontSize="19">The Feed (Beta)</TextBlock>
                <AppBarButton Icon="Add" Grid.Row="0" Label="Post" Click="AppBarButton_Click"/>
                <AppBarButton x:Name="RefreshBtn" Icon="Refresh" Grid.Row="0" Label="Refresh" Click="Refresh_Click"/>
                <AppBarButton x:Name="DailyPollBtn" Grid.Row="0" Label="Daily Poll" Click="DailyPoll_Click">
                    <AppBarButton.Icon>
                        <FontIcon Glyph="&#xE9D2;" />
                    </AppBarButton.Icon>
                </AppBarButton>
            </StackPanel>
            <ListView x:Name="FeedViewer" Grid.Row="1" IsTapEnabled="False"
                  IsItemClickEnabled="False"
                  ScrollViewer.ZoomMode="Enabled"
                  ScrollViewer.HorizontalScrollMode="Enabled"
                  ScrollViewer.VerticalScrollMode="Enabled"
                  ScrollViewer.HorizontalScrollBarVisibility="Auto"
                  ScrollViewer.VerticalScrollBarVisibility="Visible"
                  HorizontalAlignment="Stretch">
                <ListView.ItemTemplate>
                    <DataTemplate x:DataType="views:FeedUIEntry">
                        <Grid Margin="0, 0" MinHeight="25">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="auto"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>

                            <PersonPicture Margin="0,0,20,0" Grid.Column="0" Width="32" Height="32"></PersonPicture>
                            <Grid Grid.Column="1" CornerRadius="{StaticResource ControlCornerRadius}">
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="auto"/>
                                    <RowDefinition Height="auto"/>
                                </Grid.RowDefinitions>
                                <TextBlock Grid.Row="0" Text="{x:Bind AuthorAndDate, Mode=OneWay}" Margin="0, 0, 0, 0" FontSize="15" Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
                                <TextBlock Grid.Row="1" Text="{x:Bind StringContent, Mode=OneWay}" TextWrapping="Wrap" Padding="0, 0, 0, 5" FontSize="18"/>
                            </Grid>
                        </Grid>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
        </Grid>

        <StackPanel x:Name="Loader" Grid.Column="0" Grid.Row="0" Grid.RowSpan="2" Grid.ColumnSpan="2" VerticalAlignment="Center" HorizontalAlignment="Center">
            <ProgressRing x:Name="LoadingThing" Height="64" Width="64"/>
            <TextBlock x:Name="LoadingText" HorizontalAlignment="Center">Retrieving feed data</TextBlock>
        </StackPanel>
    </Grid>
</Page>