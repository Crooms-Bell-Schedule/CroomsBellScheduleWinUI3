<?xml version="1.0" encoding="utf-8"?>

<Page
    x:Class="CroomsBellSchedule.UI.Views.Settings.ProwlerView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:local="using:CroomsBellSchedule.Utils"
    xmlns:views="using:CroomsBellSchedule.UI.Views.Settings"
    xmlns:cui="using:CroomsBellSchedule.Controls"
    xmlns:animatedvisuals="using:Microsoft.UI.Xaml.Controls.AnimatedVisuals"
    mc:Ignorable="d" Loaded="Page_Loaded"
    NavigationCacheMode="Required">

    <Page.Resources>
        <Flyout x:Key="UserFlyout" Placement="BottomEdgeAlignedLeft">

            <Flyout.FlyoutPresenterStyle>
                <Style TargetType="FlyoutPresenter">
                    <Setter Property="Margin" Value="0"/>
                    <Setter Property="Padding" Value="0"/>
                    <Setter Property="CornerRadius" Value="10"/>
                    <Setter Property="BorderThickness" Value="0"/>
                </Style>
            </Flyout.FlyoutPresenterStyle>

            <!-- The flyout's DataContext must be the Image Source
             of the image the flyout is attached to. -->
            <Grid MinWidth="315">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <Image x:Name="FlyoutBanner" Grid.Row="0" Height="175" Stretch="UniformToFill" Margin="0"/>

                <PersonPicture x:Name="FlyoutPicture" Width="64" Height="64" VerticalAlignment="Top" HorizontalAlignment="Left" Grid.Row="1" Padding="0" Margin="0, -50, 10, 0" />

                <StackPanel Grid.Row="2">
                    <TextBlock x:Name="FlyoutUserName2" Text="User Account"  FontSize="22" Margin="0, 0, 0, 0" 
             Padding="12, 5, 10, 5" TextWrapping="Wrap"/>
                    <HyperlinkButton Content="View profile" Margin="10,5,0,10" Click="HyperlinkButton_Click"></HyperlinkButton>
                </StackPanel>
            </Grid>
        </Flyout>
    </Page.Resources>

    <Grid x:Name="FeedUI">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"></RowDefinition>
            <RowDefinition Height="Auto"></RowDefinition>
            <RowDefinition Height="Auto"></RowDefinition>
            <RowDefinition Height="Auto"></RowDefinition>
            <RowDefinition Height="*"></RowDefinition>
        </Grid.RowDefinitions>

        <CommandBar Background="Transparent" DefaultLabelPosition="Right" HorizontalAlignment="Left" Grid.Row="0">
            <AppBarButton Icon="Add" Grid.Row="0" Label="New" Click="AppBarButton_Click"/>
            <AppBarButton x:Name="DailyPollBtn" Grid.Row="0" Label="Daily Poll" Click="DailyPoll_Click">
                <AppBarButton.Icon>
                    <FontIcon Glyph="&#xE9D2;" />
                </AppBarButton.Icon>
            </AppBarButton>
            <AppBarButton x:Name="MAdvice" Grid.Row="0" Label="Mikhail's Advice" Click="MA_Click" Icon="PhoneBook"/>

             <CommandBar.SecondaryCommands>
                <AppBarToggleButton x:Name="AutoScrollButton" Label="Auto Scroll" Icon="AlignCenter"/>
                <AppBarButton x:Name="ClearCache" Label="Clear cache" Icon="Delete" Click="ClearCache_Click"/>
            </CommandBar.SecondaryCommands>
        </CommandBar>

        <ProgressBar x:Name="ProgressUI" Value="50" Minimum="0" Maximum="100" Grid.Row="1" VerticalAlignment="Bottom" IsIndeterminate="True">
            <ProgressBar.Resources>


                <Storyboard x:Name="ProgressBarAnimation">
                    <DoubleAnimationUsingKeyFrames Storyboard.TargetName="ProgressUI" Storyboard.TargetProperty="Value"
                                                   EnableDependentAnimation="True">
                        <LinearDoubleKeyFrame Value="0" KeyTime="0:0:0"/>
                        <LinearDoubleKeyFrame Value="55" KeyTime="0:0:2"/>
                        <EasingDoubleKeyFrame Value="99" KeyTime="0:0:30"/>
                    </DoubleAnimationUsingKeyFrames>
                </Storyboard>
            </ProgressBar.Resources>
        </ProgressBar>

        <StackPanel Grid.Row="2">
            <InfoBar x:Name="MainInfoBar" Message="In order to post on Prowler, please verify your account" IsOpen="True" Severity="Error" IsClosable="True">
                <InfoBar.ActionButton>
                    <Button x:Name="BtnVerify" Content="Verify" Click="BtnVerify_Click" HorizontalAlignment="Right"/>
                </InfoBar.ActionButton>
            </InfoBar>

            <InfoBar x:Name="DisconnectServer" Message="Disconnected from server. Attempting to reconnect..." IsOpen="True" Severity="Warning" IsClosable="False"/>
            <InfoBar x:Name="NoInternetAccess" Message="No internet access. Check your connection." IsOpen="False" Severity="Informational" IsClosable="False"/>
        </StackPanel>

        <views:PostView x:Name="Poster" Grid.Row="3" OkayClick="Poster_OkayClick" Visibility="Collapsed" ExitClick="Poster_ExitClick"></views:PostView>

        <ListView x:Name="FeedViewer" Grid.Row="4"
                  ScrollViewer.ZoomMode="Enabled"
                  ScrollViewer.HorizontalScrollMode="Enabled"
                  ScrollViewer.VerticalScrollMode="Enabled"
                  ScrollViewer.HorizontalScrollBarVisibility="Auto"
                  ScrollViewer.VerticalScrollBarVisibility="Visible"
                  SelectionMode="None" Visibility="Collapsed">

            <ListView.ItemTemplate>
                <DataTemplate x:DataType="views:FeedUIEntry">
                    <cui:ProwlerPost Date="{x:Bind Date}"
                                         Author="{x:Bind Author}"
                                         AuthorAndID="{x:Bind AuthorAndID}"
                                         AuthorId="{x:Bind AuthorId}"
                                         ContentData="{x:Bind ContentData}"
                                         CanEdit="{x:Bind CanEdit}"
                                         CanBan="{x:Bind CanBan}"
                                         Id="{x:Bind Id}"/>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
        <Grid x:Name="LoadingScreen" Grid.Row="4">
            <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                <TextBlock x:Name="LoadingStatus" FontSize="38" HorizontalAlignment="Center">Loading Prowler data...</TextBlock>
                <TextBlock x:Name="LoadingTip" VerticalAlignment="Bottom" HorizontalAlignment="Center" FontSize="28" FontWeight="Light"
                               Margin="0,50,0,0" TextWrapping="WrapWholeWords">Tip: Light mode attracts bugs</TextBlock>
            </StackPanel>
        </Grid>
    </Grid>
</Page>