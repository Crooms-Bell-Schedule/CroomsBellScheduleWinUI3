<?xml version="1.0" encoding="utf-8"?>

<Page
    x:Class="CroomsBellSchedule.UI.Views.Settings.ProwlerProfileView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:controls="using:Microsoft.UI.Xaml.Controls"
    
    xmlns:cui="using:CroomsBellSchedule.Controls"
    mc:Ignorable="d">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Grid Grid.Row="0">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <Image x:Name="FlyoutBanner" Grid.Row="0" Height="250" Stretch="Fill" Margin="0"/>

            <PersonPicture x:Name="FlyoutPicture" Width="64" Height="64" VerticalAlignment="Top" HorizontalAlignment="Left" Grid.Row="1" Padding="0" Margin="0, -50, 10, 0" />

            <StackPanel Grid.Row="2">
                <TextBlock x:Name="FlyoutUsername" Text="User Account"  FontSize="28" Margin="0, 0, 0, 0" 
             Padding="12, 5, 10, 5" TextWrapping="Wrap"/>
            </StackPanel>
        </Grid>


        <Grid Grid.Row="2">
            <Grid.RowDefinitions>
                <RowDefinition Height="auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <TextBlock Grid.Row="0" FontSize="20"   Padding="12, 5, 10, 5" >Post history</TextBlock>

            <ListView x:Name="FeedViewer" Grid.Row="1"
               ScrollViewer.ZoomMode="Enabled"
               ScrollViewer.HorizontalScrollMode="Enabled"
               ScrollViewer.VerticalScrollMode="Enabled"
               ScrollViewer.HorizontalScrollBarVisibility="Auto"
               ScrollViewer.VerticalScrollBarVisibility="Visible"
               HorizontalAlignment="Stretch"
 xmlns:views="using:CroomsBellSchedule.UI.Views.Settings"
               SelectionMode="None" Visibility="Visible">

                <ListView.ItemTemplate>
                    <DataTemplate x:DataType="views:FeedUIEntry">
                        <cui:ProwlerPost Date="{x:Bind Date}"
                                      Author="{x:Bind Author}"
                                      AuthorAndID="{x:Bind AuthorAndID}"
                                      AuthorId="{x:Bind AuthorId}"
                                      ContentData="{x:Bind ContentData}"
                                      CanEdit="{x:Bind CanEdit}"
                                      CanBan="{x:Bind CanBan}"
                                      Id="{x:Bind Id}"/>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
        </Grid>
        <ProgressRing x:Name="LoadingRing" HorizontalAlignment="Center" VerticalAlignment="Center"/>
    </Grid>
</Page>